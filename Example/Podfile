source 'https://github.com/CocoaPods/Specs.git'
use_frameworks!

target 'TLCachedAutoHeightCell_Example', :exclusive => true do
  pod "TLCachedAutoHeightCell", :path => "../"
end

target 'TLCachedAutoHeightCell_Tests', :exclusive => true do
  pod "TLCachedAutoHeightCell", :path => "../"

  pod 'Specta'
  pod 'Expecta'
  pod 'FBSnapshotTestCase'
  pod 'Expecta+Snapshots'
end
